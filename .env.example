# ===========================================
# LOF 溢价监控服务 - 环境变量配置示例
# ===========================================
# 复制此文件为 .env 并填入实际值
# 注意：.env 文件包含敏感信息，不要提交到 Git
# ===========================================

# 集思录账号（必填）
JISILU_USERNAME=your_jisilu_username
JISILU_PASSWORD=your_jisilu_password

# API 访问令牌（必填）
# 用于微信小程序访问 API 时的身份验证
# 建议使用 openssl rand -hex 16 生成
API_TOKEN=your_secure_api_token

# PostgreSQL 数据库配置
POSTGRES_USER=lof_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=lof_monitor

# 数据库连接字符串（自动拼接，无需修改）
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@lof-postgres:5432/${POSTGRES_DB}

# 时区
TZ=Asia/Shanghai

# 抓取配置（可选，有默认值）
# SCRAPE_MIN_INTERVAL=50  # 最小抓取间隔（分钟）
# SCRAPE_MAX_INTERVAL=70  # 最大抓取间隔（分钟）
# SCRAPE_START_HOUR=7     # 开始抓取时间（小时）
# SCRAPE_END_HOUR=24      # 结束抓取时间（小时）
